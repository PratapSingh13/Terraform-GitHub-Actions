apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: ebs-storage-class
provisioner: ebs.csi.aws.com
parameters:
  type: gp3  # General Purpose SSD (recommended for production)
  fsType: ext4
allowVolumeExpansion: true
